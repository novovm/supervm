[package]
name = "novovm-exec"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
anyhow = "1"
aoem-bindings = { path = "../aoem-bindings" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
