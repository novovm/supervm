# WEB30 Reference README

> Historical source snapshot / archived reference.
>
> This directory preserves migrated `SVM2026` source material. Legacy `SuperVM`
> wording inside the snapshot is source-era wording, not current NOVOVM external
> naming.
> Legacy `SuperVM` wording is not current NOVOVM external product naming.
> Current external naming remains NOVOVM; `SUPERVM` is retained only as
> repository/path/internal historical code name during migration.

This compatibility file points to the canonical snapshots:

- `WEB30-README.md`
- `STANDARDS-INDEX.md`

Standards source snapshot directory:

- `standards/`
